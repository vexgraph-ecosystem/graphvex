#ifndef GRAPHVEX_VERSION_H
#define GRAPHVEX_VERSION_H

// graphvex/version.h — skeleton placeholder for the bespoke graphics layer.
// (Foundation layer: vexspoke-only includes. See CMakeLists.txt.)

const char *Graphvex_version(void);

#endif
